package arrayList;import java.util.ArrayList;import java.util.Collections;import java.util.List;/** * can contains duplicate element and insertion order is preserved * heterogeneous data * Can contain null * Not Thread Safe * implements List Interface * * Empty arraylist size is 10 * if it exeed the limit then all the elements are copied to another and the new array size is * (currentCapacity * 3/2)+1 * * Best choice for frequent operation is retriaval */public class ArrayListExample1 {    ArrayList<String> emptyArraylist = new ArrayList<>(); // its capacity is 10, size is 0    ArrayList<String> arraylist = new ArrayList<>(12);    ArrayList<String> al = new ArrayList();    List synchronizedList = Collections.synchronizedList(al);    public static void main(String...args){    }}